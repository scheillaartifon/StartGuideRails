<div class="nested-fields">
    <div class="field">
        <p>
            <%= f.label :nome %>
            <%= f.text_field :nome %>
        </p>

        <p>
            <%= f.label :descricao %>
            <%= f.text_field :descricao %>
        </p>
        <button type="button" class="btn btn-primary d-flex mt-3 mb-4 ml-auto">
            <%= link_to_remove_association "Remove Item", f %>
        </button>
    </div>
</div>